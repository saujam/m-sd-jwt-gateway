# M-SD-JWT Gateway

**Merkle-tree-based SD-JWT framework for privacy-preserving SCIM pagination**

Official prototype for the paper submitted to Springer *Cluster Computing*.

## Quick Start
```bash
git clone https://github.com/saujam/m-sd-jwt-gateway.git
cd m-sd-jwt-gateway
go mod tidy
go run main.go